window.__FINECO_CONFIG__ = {
  // Vercel proxy handles /api -> https://finly.uyqidir.uz/api server-side.
  // Leave apiBaseUrl unset so environment.prod.ts fallback ('/api') is used.
  // Set this in production for AI chatbot.
  // groqApiKey: 'gsk_...'
};
